import { noop } from 'src/utils/common';

export const initialize = noop;
export const checkNotificationPermission = noop;
export const requestNotificationPermission = noop;
export const scheduleLocalNotification = noop;
export const cancelAllScheduledNotifications = noop;
